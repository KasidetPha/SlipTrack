# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.22,<0.32

# MySQL (async)
aiomysql>=0.2.0,<1.0

# Auth / JWT
PyJWT>=2.8,<3.0

# Env variables
python-dotenv>=1.0,<2.0

# (FastAPI ใช้ Pydantic v2)
pydantic>=2.5,<3.0
bcrypt>=4.0,<5.0